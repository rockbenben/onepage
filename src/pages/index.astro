---
import Page from "../layouts/Page.astro";
import { locales, siteFor } from "../lib/data";

// 住在 / 的是主语言（主文件的「语言」，默认 zh-CN）；其余语言按翻译文件自动发现（见 locales.ts）
const primary = locales[0];
---

<Page site={siteFor(primary.code)} locales={locales} current={primary.code} />
