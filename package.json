{
  "name": "onepage",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "一个文件，就是你的主页 · 365 开源计划 #022",
  "scripts": {
    "test": "vitest run",
    "validate": "node scripts/validate-config.mjs",
    "fetch": "node scripts/fetch-github.mjs",
    "dev": "npm run validate && astro dev",
    "build": "npm run validate && npm run fetch && astro build",
    "build:offline": "npm run validate && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/preact": "4.1.0",
    "astro": "^5.0.0",
    "preact": "10.27.2"
  },
  "devDependencies": {
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@fontsource/inter": "^5.2.8",
    "@tailwindcss/vite": "^4.0.0",
    "js-yaml": "^4.1.0",
    "sharp": "^0.35.3",
    "tailwindcss": "^4.0.0",
    "vitest": "^4.1.9"
  }
}
